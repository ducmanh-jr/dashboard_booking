{
  "scripts": {
    "check": "pnpm -r run check",
    "build:web": "pnpm --filter nowayhome-web run build:admin && pnpm --filter nowayhome-web run build:partner && pnpm --filter nowayhome-web run build:customer",
    "db:migrate": "pnpm --filter backend exec prisma migrate deploy --schema prisma/schema.prisma",
    "db:export": "powershell -ExecutionPolicy Bypass -File database/snapshots/export.ps1",
    "db:reset": "powershell -ExecutionPolicy Bypass -File database/baseline/import.ps1",
    "db:migrate:legacy-dry": "node database/migration/migrate-legacy-snapshot.mjs --dry-run",
    "db:migrate:legacy": "node database/migration/migrate-legacy-snapshot.mjs --reset"
  }
}
